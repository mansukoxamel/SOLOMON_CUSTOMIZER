# TODO

## 保存前チェック整理

- 既存の保存前エラー/署名チェックを棚卸しする。
- どのモジュールが何を検査しているか整理する。
- 個別に散らばっているチェックを、共通の「保存前チェック結果」に寄せられるか検討する。
- 正常時はユーザーに余計な表示を出さず、必要ならステータスバー/ログだけにする。
- 異常時だけ、ユーザー向けの短い説明とログ向けの詳細を出す。
- 特に `PanelMonsterStageVariantError` など、予約領域やruntime署名の競合が起きた時に、どこが何と競合したのか読みやすくする。

## 音楽データ表示/変換アイディア

- まず `docs/sound_analysis.html` を再確認し、ROM内の曲データ、トラック、音高、長さ、休符、ループ、テンポの仕組みを整理する。
- 最初は五線譜ではなく、テキストで表示する。
- 表記は `C D E F G A B` ベースにする。
- `#` / `b`、オクターブ、音符の長さ、休符、ループ位置を表示できるようにする。
- 可能なら raw command と interpreted note を並べて、解析ミスを見つけやすくする。
- 表示できるようになった後、変換機能を検討する。
- 変換候補:
  - 半音上げ/下げ
  - メジャー/マイナー寄せ
  - 3度上ハモリ生成
  - テンポ変更
  - 一部トラックだけ差し替え
