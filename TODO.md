# TODO

## 落下死で妖精化する敵のステージ別指定

- 原作の「敵リスト1体目」固定の特殊処理を直接ON/OFFするのではなく、鍵持ち敵と同じ方向性で、ステージごとに「落下死で妖精化する敵番号」を指定できる形を検討する。
- 対象は落下死で妖精化する処理だけに限定する。火球で倒した時や通常ドロップは別扱いにする。
- 設定値はステージデータ側へ保存し、部屋開始時にStageExt系の仕組みでRAMへコピーして実行時処理が参照する方式を基本にする。
- 実行時処理は原作の落下死→妖精化ルートに結線する。bank0実行中にPRG1コードを直接呼ばないこと。
- 鍵持ち敵と同じ敵には妖精化を設定できないようにする。
- UIは敵の `#` 番号で指定するプルダウンにする。`なし` と、その面の初期配置敵数までを候補にする。

## 改造ROM差分比較ツール

- 作成済みの改造ROM同士、たとえばバージョン1とバージョン2を比較し、どこがどれくらい違うか分かるツールを検討する。
- 単純なファイルCRCやバイナリ一致だけではなく、ステージデータ、ROM設定、追加runtime/table、メタデータなどの差分を分類して表示する。
- 結果は「Stage 8: アイテム1件変更」「Panel Variant設定2件変更」「PRG runtime差分あり」のように、確認しやすい粒度でまとめる。
- 可能なら詳細表示で、変更されたステージ番号、座標、変更前/変更後の値、ROM offsetなどを確認できるようにする。
- リリース前後や検証ROM同士を比較して、意図しない変更が混ざっていないか確認する用途を想定する。

## ステージPNG論理比較ツールアイデア

- ステージPNG同士、または現在のステージとステージPNGを比較し、同じステージデータかどうかを判定できるツールを検討する。
- PNGファイル全体のCRCやバイナリ一致では判定しない。PNGには保存日時、アプリバージョン、圧縮差、埋め込みメタ情報などが入り、同じステージでもファイルとしては一致しないため。
- 比較時はPNG内の埋め込みステージデータ/XMLを読み出し、論理データとして正規化して比較する。
- 比較対象の例: ブロック配置、アイテム配置、敵配置、メタ/特殊配置、タイルセット、制限時間、ステージ設定、鍵敵、ミラー設定、パネルモンスター設定など。
- 無視する情報の例: 保存日時、アプリバージョン、ファイル名、PNG画像自体のCRC、表示用画像の圧縮差やメタ情報。
- 結果は大げさな画面にせず、まずは「同一ステージデータです」または「変更あり: ブロック3箇所、敵1箇所、暗闇ON->OFF」のような短い差分表示を目標にする。
- 可能なら差分の座標 `(x, y)` も出し、どこが変わったか確認しやすくする。
- 将来的にはPNG保存時に論理ステージCRCを埋め込む案もあるが、既存PNGとの互換を考えて、最初は埋め込みXMLの正規化比較を優先する。

## Undo/Redo監査アイデア

- 既存のUndo/Redo対象を棚卸しし、ステージ編集操作で取り漏れがないか確認する。
- 面コピー、貼り付け、入れ替え、配置、削除、ステージ設定変更など、ユーザーが「戻せる」と期待する操作を優先して確認する。
- ROM全体変更や共通設定など、無理にUndo対象へ入れない方がよい操作は、確認表示やログで事故を防ぐ方向にする。

## 変更サマリー/変更追跡アイデア

- 大げさな画面は作らず、どのステージやROM設定が未保存かを軽く分かるようにする案。
- ステージごとの変更状態を持ち、単品ステージPNG保存ではそのステージだけクリア、全ステージPNG保存ではステージ分をまとめてクリアすることを検討する。
- ROM保存ではステージ変更とグローバル/ROM設定変更をまとめてクリアすることを検討する。
- 表示する場合はステータスバーや短いログ程度にして、ユーザーに余計な確認を増やさない。

## 音楽データ表示/変換アイデア

- まず `docs/sound_analysis.html` を再確認し、ROM内の曲データ、トラック、音高、長さ、休符、ループ、テンポの仕組みを整理する。
- 最初は五線譜ではなく、テキストで表示する。
- 表記は `C D E F G A B` ベースにする。
- `#` / `b`、オクターブ、音符の長さ、休符、ループ位置を表示できるようにする。
- 可能なら raw command と interpreted note を並べて、解析ミスを見つけやすくする。
- 表示できるようになった後、変換機能を検討する。
- 変換候補:
  - 半音上げ/下げ
  - メジャー/マイナー寄せ
  - 3度上ハモリ生成
  - テンポ変更
  - 一部トラックだけ差し替え

## UI英語化/多言語対応

- 現在のUIは日本語中心なので、英語表示に切り替えられる仕組みを検討する。
- 最初は日本語/英語の2言語でよい。
- メニュー、ボタン、ダイアログ、ステータスバー、エラーメッセージ、ツールチップを対象にする。
- 既存の保存データや設定キーは変えず、表示文字列だけを切り替える。
- まず文字列の棚卸しを行い、翻訳テーブルまたはQt翻訳機構のどちらが扱いやすいか比較する。

## IPSパッチ適用/配布方式の検討

- 現行のIPS出力に加えて、アプリ内でIPSを適用する機能を検討する。
- IPS標準仕様だけでは元ROM CRCを保証できないため、適用前に元ROM CRC32を確認する運用を考える。
- IPS出力時に、想定元ROM名、元ROM CRC32、出力ROM CRC32、アプリバージョン、作成日時などをメタ情報として残す方法を検討する。
- 例: `patch.ips` と一緒に `patch.skc_patch.json` を出力し、IPS適用時に横のメタJSONがあればCRC照合する。
- メタ情報が無いIPSを適用する場合は、警告を出すか、日本版確認済みCRCだけを許可するか検討する。
- IPSで十分か、BPSなどチェックサムを持てる形式も候補に入れるか比較する。




## ROM保存時のバリデーション

- ROM保存時に、ステージ構成上の注意点を確認する整合性チェック機能を検討する。
- 例として、4の倍数ステージに対応する星座マークが配置されているか確認する。
- 星座マークは単に配置されていればよいわけではなく、該当ステージに対応する星座かどうかも確認対象にする。
- 制作者の意図で星座マークを出さない、または対応星座と異なる配置にする場合もあり得るため、エラーではなく注意喚起として扱う方向で検討する。
- 星座マークだけでなく、背景として使う星座パネルも想定通り一通り使われているか確認する案を検討する。
- ソロモンの封印をユーザーが移動できるようにする場合、原作分岐ロジックに沿ったガードを入れる。
- 封印の累計数 `$79` が分岐条件なので、4個目までを20面クリア前、6個目までを44面クリア前、8個目までを48面クリア前に取得可能な配置だけを許可する。
- 封印のbit種別自体は分岐条件ではなく、取得済みbitfield `$7A` による再配置抑止に使われる。同じbitを複数面へ置いても取得数稼ぎにはならない。
- ロジック上は同じ面に複数の異なる封印bitを出すことや、序盤に8個すべてを出して取らせることも成立し得る。UIで許可するか、原作寄りの制限を追加するかは別途決める。
- 星座パネル側は `$78 bit3` の一時フラグで、該当面で取ってクリアした時だけ効く。20面/44面/48面の分岐検証では、封印数だけでなくその面のパネル取得も別条件として扱う。



## 全ステージ統計の表示順

- 配置敵と重要アイテムの並び順を整理する。


## ANDマーク追加の検討

- ANDマークを追加できるか検討する。
- 追加に使えるROM内の空き領域があるか確認する。

## 原作のステージ開始直後に敵が逆さに見える件の検証メモ

- 原作ROMでも、特定ステージ開始直後に敵が1フレーム程度逆さまたは異常姿勢で見えることを確認した。
- `TEST_EnemyStartNoInitialVFlip_D94F_OriginalJP.nes`: `$D951/$D954` の `$ED->$DE` は、反転前提のタイルを通常向きで出してしまい、スプライトが上下に崩れて見えたため不採用。
- `TEST_EnemyStartSkipD94F_EDFrames_OriginalJP.nes`: `$D94F` の先頭EDフレームを飛ばして `$D955` 開始にすると、別モンスターのような見た目になったため不採用。
- `TEST_EnemyStartDelayActiveSet_OriginalJP.nes`: `$95CA` の早期 `status=$80` 書き込みをNOP化する検証も、見た目の問題解消にはつながらなかった。
- 現時点ではアニメデータ単純変更や早期active抑制では決め手なし。再開する場合は、原作挙動として許容するか、最初の数フレームだけスプライト非表示にする専用処理を別途検討する。
- 当面は仕様外として扱い、同じ検証を繰り返さないようにする。

## ステージ開始時の不具合メモ

- なぜかガーゴイルが逆方向に動くケースがあるため、確認する。



## タイトル画面への配置要素追加

- タイトル画面にアイテムやキャラクターを配置できるか検討する。
- 現時点の主な要望は、キャラクターではなくアイテムをタイトル画面へ配置すること。
- タイトル画面のCHR bank3にはアイテムのように見える絵があるが、背景込みのタイトル用タイルとして焼き込まれており、ゲーム中スプライトのような透明背景付きの独立アイテム素材ではない。
- そのため、bank3内の既存タイルをそのまま置くだけで「アイテムだけ」を自然に配置するのは難しい。
- 実現する場合は、アイテム部分を切り抜いた新規タイトル用タイルを作る、未使用CHR枠へ追加する、タイトルBGパレット/attribute制約に合わせる、配置UIを用意する、といった重い作業になる。
- 簡単な追加機能として扱わず、必要になった場合はタイトル用素材作成を含む別タスクとして検討する。



## タイトル取り込みまわりの不具合メモ

- タイトル取り込み時に不具合が出るケースがあるため、再現条件を確認する。
