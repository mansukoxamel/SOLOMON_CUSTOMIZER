# Codex 作業メモ

- ユーザーは音声入力を使うことが多い。日本語の変換や聞き取りが多少崩れていても、文脈と意図を優先して解釈する。正しさや安全性に影響する曖昧さだけ、短く確認する。

## Panel Monster v2 runtime ブランチの最優先ルール

- 合言葉: 「まともに動くものを作るまで、互換・救済・将来対応を入れない」。
- `codex/panel-monster-v2-runtime` では、最新の「ROMを作る」で出るROMを成立させることを最優先にする。
- 古い実験ROM、途中生成ROM、未配布ROMを救うための互換処理を追加しない。必要になってから別作業で扱う。
- cache方針の合言葉: 「cacheは残す。PRG0を削る」。PRG1とRAMは荷物置き場、PRG0は狭い通路として扱う。
- 作業説明は毎回短く、今の目的を一言で言う。例: 「今はPRG0の余計な書き込みを止める」。

## プロジェクト作業ルール

- プロジェクトファイルを編集する前に、変更対象ファイルの編集前状態を `BUP/` 配下へバックアップする。
- 編集したファイルが.pyの場合は「アプリのバージョン更新」「アプリの`CHANGELOG.md`へ追記」も行う。
- ファーストリリース後は、ユーザー作成データの保存形式を安定版として扱う。特に共通/グローバル設定JSONとステージデータ（PNG埋め込みXML、および将来のXML同時出力）は、原則として既存スキーマ、キー名、属性名、値の意味を変更しない。
- 保存形式をどうしても変更する必要がある場合は、同じ変更で旧形式を読み取り現行形式へ変換する互換読込（マイグレーション）を実装する。読み込み後に現行形式で再保存してよいが、過去バージョンでユーザーが保存した共通/グローバル設定JSONやステージデータを黙って読めなくしない。
- 互換維持の対象は、ユーザーが保存・再利用するプロジェクトデータ、共通/グローバル設定JSON、ステージデータ、表示ラベル等をキーとして保存する設定、ROM内のアプリ独自メタデータとする。対象外は、一時テスト出力、開発中の調査メモ、ユーザーへ渡していない内部実験ファイルとする。
- ROM配置は通常編集対象の日本版 mapper66 wide-title 拡張ROMについて、ファーストリリース後の保存ROMを再読込・再保存できることを基本とする。ROM/RAM配置を変更する場合は、管理簿と実装側の予約を更新し、既存データからの移行または再適用方法を同じ変更内で明確にする。

## 日本語ファイルと文字コードの安全ルール

- 日本語を含む可能性があるファイルは、PowerShellの既定表示を信用しない。`Get-Content -Encoding UTF8` で読み直してから判断する。
- 日本語を含むファイルを PowerShell の `Set-Content`、リダイレクト、スクリプト生成などで書き換えない。手編集は原則 `apply_patch` を使う。
- パッチのアンカーには日本語コメントや日本語文字列を使わない。関数名、変数名、定数名、ASCIIだけの構文行をアンカーにする。
- 文字化けして見えた状態の内容を根拠に編集しない。表示が怪しい時は、UTF-8指定で読み直すか、ASCIIの構造だけを使って編集する。
- 日本語コメントや説明文を含むファイルを編集した後は、対象範囲を `Get-Content -Encoding UTF8` で確認し、コメントが消えたり文字化けしたりしていないことを確認する。
- 大きな機械的置換で日本語を含むファイル全体を書き直さない。必要最小限の差分だけを当てる。
- 文字化け、コメント消失、意図しない大規模差分を見つけたら、そのまま作業を進めない。編集前バックアップを確認し、壊した変更を修正してから次へ進む。

## ROMサポート方針

- このカスタマイザーの通常編集対象は、このアプリが生成する日本版 mapper66 wide-title 拡張ROMだけ。
- 生の日本版ROMは入力元であり、読み込み時に日本版 mapper66 wide-title 拡張形式へ変換される。編集処理は変換後の形式を前提に設計する。
- US ROMは通常編集対象ではない。タイトル画面素材の取り込みなど、限定的な素材元としてだけ扱う。
- ユーザーが別目標として明示しない限り、US ROM編集互換の維持や保守に実装リソースを使わない。

## 原作ROM解析資料の扱い

D:\chaos\MAGATU_EMULATOR\SOLOMON_CUSTOMIZER\解析資料\ROM完全解析\solomon_commented.asm
D:\chaos\MAGATU_EMULATOR\SOLOMON_CUSTOMIZER\ROM\Solomon no Kagi (J).nes

- 原作ROMのアセンブリ挙動を確認する場合は、まず上記のコメント付きASMを一次参照として確認する。特殊処理、アイテム配置、敵処理、ROM/RAMアドレスの意味を調べる時の最重要資料として扱う。
- 実ROMバイトを確認する場合は、上記の日本版原作ROMを参照する。
- ただし、このASMコメントには古い推測や誤記が混じることがある。確定判断は、コメント付きASM、実ROMバイト、実行時挙動、現在の実装を突き合わせて行う。
- コメント付きASMに誤りを見つけた場合や、新しく確定した解析結果が出た場合は、対象ファイルを `BUP/` にバックアップしてから、そのASMコメントへ修正・追記する。
- このコメント付きASMは原作ROM由来の解析資料であり、GitHubへ載せてはいけない。Git管理対象に追加しない。関連バックアップもGitへ含めない。
- 解析資料を編集する場合も、日本語ファイルと文字コードの安全ルールを守る。PowerShellで内容確認する時は `Get-Content -Encoding UTF8` を使い、手編集は原則 `apply_patch` で最小差分にする。

## ROM/RAM管理簿
D:\chaos\MAGATU_EMULATOR\SOLOMON_CUSTOMIZER\docs\ram_map_current.html
D:\chaos\MAGATU_EMULATOR\SOLOMON_CUSTOMIZER\docs\rom_map_jp_mapper66_current.html

- ROM/RAMアドレスを新しく使う前に、必ず上記2つの管理簿と実装側の `RESERVED_SPANS` を確認する。
- 「空いていそうだから試す」「動いたから採用」は禁止。根拠が管理簿に無いアドレスは未確定として扱う。
- 新しいROM/RAM予約、既存予約の移動、予約解除をしたら、同じ変更で管理簿も更新する。重複が見つかった状態では実装完了にしない。
