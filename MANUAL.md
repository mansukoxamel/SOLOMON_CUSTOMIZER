# SOLOMON_CUSTOMIZER マニュアル

ファミコン版『ソロモンの鍵』ステージエディタ - [skchain](https://github.com/kaimitai/skchain) (C++製) を Python + PyQt5 に移植＋独自拡張。

## 特徴

- 全53ステージ可視化＋編集
- US/JP/EU/拡張ROM(US66/JP66) 自動判別
- ブロック・アイテム・敵・メタ要素の配置編集
- 通常ROM (.nes) / IPSパッチ (.ips) / PNG画像 出力対応
- skchain 互換のXML設定ファイル流用

## 必要環境

- Python 3.10+
- PyQt5

```
pip install PyQt5
```

## 起動

```
python SOLOMON_CUSTOMIZER.py
```

または .nes ファイルをドラッグ&ドロップで起動:

```
python SOLOMON_CUSTOMIZER.py path/to/Solomons_Key.nes
```

## 基本操作

1. **ROMを開く...** ボタンで `.nes` ファイル選択
2. **ステージ選択** スピンボックスかリストで編集対象ステージ選択
3. **編集対象** を選んで（ブロック/アイテム/敵/メタ）
4. **左クリック**で配置、**右クリック**で削除
5. **改造ROMとして保存...** か **IPSパッチ出力...** で保存

## ゲーム挙動改造メモ

- **ワープ羽**: 原作では取得後のクリアで6面分進みます。ゲーム挙動改造
  ダイアログから、この進行数を変更できます。
6. 各種**画像エクスポート**で PNG 出力

## キーボードショートカット

| キー | 機能 |
|---|---|
| F1 | キーボードショートカットMAP表示 |
| Ctrl+F1 | 本マニュアル表示 (notepad) |
| F9 | 設定画面（フォント・アイコン・エミュレータ等） |
| F10 | アプリ再起動 |
| Ctrl+Z | Undo（編集を取り消し、最大50件） |
| Ctrl+Y / Ctrl+Shift+Z | Redo（やり直し） |
| PageUp / PageDown | ステージ切替 |
| G | グリッド表示ON/OFF |
| 1〜9, 0 | お気に入りスロット選択（D&Dで登録、Delで削除） |

## マウス操作

| 操作 | 機能 |
|---|---|
| 左クリック | 選択中の要素を配置 |
| 右クリック | そのタイルの全要素を削除（編集モード非依存。アイテム・敵・ブロックを全て消す。メタ要素は対象外） |
| Ctrl+左ドラッグ | 既存要素を掴んで移動。Ctrl押下中はマウスに追従、Ctrl解放 or マウスボタン解放で確定（アイテム/敵/鍵/扉/スタート/ミラー対応） |

## 編集対象

### ブロック
- 消去（空白）
- 茶色ブロック（壊せる）
- 白ブロック（壊せない）

（v0.1.99 で「壊せる白ブロック」(BROWN_WHITE) は廃止。実機で通常の白壁と同挙動の冗長表現と判明したため。）

### アイテム
ベル、宝石、時間ボトル、巻物など20種以上

**配置時のフラグ**: 「アイテム」モード選択時、ピッカー上部に「通常 / 隠し / ブロック内」ラジオあり。
- 通常: そのまま見える
- 隠し (0x40): プレイヤーが魔法ブロックを生成→破壊で出現
- ブロック内 (0x80): ブロック破壊で出現

**自動レギュレーション**: Warp / Origami Swan / Demonhead Coin / Sphinx / Egyptian Head / Magic Lamp は選択時に自動で「隠し」になる。

### 敵
ゴーレム、ガーゴイル、ゴースト、ニュール、火球など20種以上

**スピードフラグ（SP1/SP2/SP3）**: 対応する敵のみピッカー上部のラジオで選択。配置時に速度バリアントが自動適用される（例: ドラゴンSP1=0x68、SP2=0x6c）。

**noslow版（Ghost/Neul）**: 0x40-0x4F のnoslow版（壁衝突時に減速しない凶悪版）もピッカーに登録済み。通常版0x30-0x3Fと並べて選択可能。

### メタ
プレイヤースタート位置、鍵位置、扉位置、ミラー1/2 位置

### 画面ごとの挙動改造 (Room Flag Table 拡張)
ステージ設定パネルの「挙動改造」で、その画面(部屋)だけに効く改造を
設定できます。原作のステージデータは一切壊しません。

- **この画面でBファイア禁止 (A換石は可)**: その部屋ではBボタンの
  火球(魔法)が出なくなります。Aボタンの石生成は通常どおり使えます。
- **この画面でA換石(石作成)禁止**: その部屋ではAボタンの石生成を
  無効化します。Bファイア禁止とは独立して設定でき、両方ON/片方だけ
  も可能です。※石で階段が作れず進行不能になり得るため、意図して
  使う設定です。
- **この画面の扉を隠す (石を壊すと出現)**: エディタで設定した扉位置の
  マスを「隠し」化します。開始前のステージ紹介画面に扉が出ず、
  ゲーム中も最初は見えませんが、その上にできた石を壊すと扉が現れます
  (原作の隠し鍵と同じ仕組み)。扉位置を動かすと自動で追従します。
- **この画面を暗闇にする (明滅・敵とDanaのみ)**: その面のプレイ中だけ
  背景(地形・HUD)を明滅で消し、敵とDanaだけ見えるようにします。
  「明」の一瞬に地形・鍵・扉が見えるので、それを覚えて暗い間に進む
  暗闇ステージになります。タイトル/ステージ紹介/クリア画面は通常
  表示で、必ず「明」から始まります。明/暗の長さは全体共通で、
  ゲーム挙動改造の「暗闇テンポ」で設定します(部屋別ON/OFFはこの
  チェック、テンポは全体共通)。
  ※v0.6.25 で、暗闇面中に妖精を取ると効果音が無限ループする不具合を
  修正しました(内部RAMの配置がサウンドと衝突していたのを移設)。

設定は ROM保存・テストプレイ時に bank0 のコードケーブへ自動注入され、
位置+署名のダブル検証に通った場合のみ書き込まれます(改造ROM/破損時は
エラーで保存中止)。全画面でOFFにすると原作フックへ自動復元します。
プロジェクト(XML)にも保存されます。

## ROM自動拡張 (mapper 66)

標準ROM (mapper 3) を読み込むと自動的に拡張ROM (mapper 66) に変換されます。これにより:

- アイテム配置数の制限がほぼ撤廃（標準ROMの容量制約を回避）
- 敵は引き続き15体まで（拡張ROMでも変わらず）
- 元ROM情報は内部保持され、IPSパッチ生成は元ROMベースで作られる

## 特殊処理ビューワ

「編集ツール」→「特殊処理ビューア...」で、各ステージ固有の特殊処理（6502アセンブリ）を確認できます。

| マーカー | 効果 |
|---|---|
| 緑枠 | 壊せる隠しブロック（無条件配置） |
| ピンク枠 | プレイヤーアクション待ちのトリガー位置 |
| 黄色枠 | トリガー後に出現する条件付きブロック |
| MBJ半透明 | マイティ・ボムジャック出現位置 |

「表示オプション → 特殊処理マーカー表示」で Canvas 上の表示ON/OFF可能。

## パレット編集

「編集ツール」→「パレット編集...」で、8パレット × 各3色を編集可能。NES 64色から選択。リアルタイムプレビュー対応。

## スプライト/キャラクタービューア

「編集ツール」→「スプライトビューア...」で、ゲーム内グラフィックを一覧表示できます（読込専用）。2モード:

### ★ROMフレームデータモード（推奨・既定）
ROM が実際にスプライト描画に使う $D0E8 テーブル機構から、全 1391
フレームを直接抽出して 16x16 で表示します。skc_config の定義に依存
しないため、エディタ用設定に無いスプライト（主人公の死亡/しゃがみ
など）も漏れなく表示されます。

- CHRバンク: 0〜3 切替（場面でどのバンクか変わる）
- パレット: SPR#0〜3 / attr&3 自動
- 拡大: 1〜8倍
- ラベル gGGsSSfF = group/state/frame
- ※ NES 8x16スプライト方式（左=tile1 / 右=tile2）で本来の姿

### キャラクターモード
skc_config のメタタイル定義を使い、複数の 8x8 タイルを組み立てた「実際のキャラクターの姿」で表示します。

- カテゴリ: アイテム / 敵 / メタ / 全メタタイル
- タイルセット: 描画に使う CHR バンク+パレットの組（0〜14）
- 拡大: 1〜12倍
- 各キャラに名前（item_desc / enemy_desc）を併記

### 生CHRタイルモード（上級者向け）
CHR-ROM の 8x8 タイルを素のまま並べて表示します。

- バンク: mapper3 の CHR バンク (8KB=512タイル単位)
- パレット: ROM 内 8 パレット (BG#0-3 / SPR#0-3)
- 拡大: 1〜16倍 / グリッド線 ON/OFF

CHR の位置は iNES ヘッダから自動算出します (JP版は 0x8010 から 2048 タイル)。
※ 個々の 8x8 タイルだけではキャラに見えません。組み上がった姿はキャラクターモードで確認してください。

## 全ステージ統計

「全ステージ統計」ボタンで、全53ステージの配置状況を一覧表で確認できます。

- 各列: 通常/隠し/ブロック内アイテム数、敵数、**タイル**(タイルセット番号)、**時間減少**(時間減少率)、**敵寿命**(スポーン敵の生存時間)、鍵の状態、星座、**A禁止 / B禁止 / 暗闇 / 隠し扉**(部屋フラグ)、配置敵、ミラー敵、重要アイテム
  - 「タイル/制限時間/敵寿命」はステージ設定パネルと同じ値です
  - 部屋フラグ列は、その画面に該当の挙動改造が設定されていると ● + 薄い着色で表示します
  - 座標表示・ミラー列は廃止しました(必要な情報に絞った構成)
- **敵は「配置敵」「ミラー敵」の2列でスプライト表示**(方向違い・速度違いは同じモンスターとして合算):
  - **配置敵**: 面に配置された敵。実数をサムネイル右下に ×N 表示
  - **ミラー敵**: デーモンミラーから出てくる敵。出現数は不明なため種類ごとに1として表示(個数表示なし)。出現タイミング表(スケジュール)に1つもチェックが無いミラーは実際に出ないため表示しません
  - マウスを乗せると名前内訳が出ます
- **重要アイテム列はスプライトのみで表示**(文字は表示しません): 各アイテムのドット絵を横並びで表示。枠色で配置状態を表現(隠し=黄/ブロック内=緑/通常のみ=灰)、同種が複数あるとサムネイル右下に個数
  - 集計対象は 紋章/Warp/Shrine/Origami Swan/Demonhead Coin/Sphinx/Egyptian Head/Magic Lamp/E-bottle のみ(コイン/宝石/Bell/Scroll/タイマー系などは除外)
  - サムネイルにマウスを乗せるとテキスト内訳(例: `Sphinx[通1]`)が出ます
- 文字は「…」で省略されず全文表示されます
- セルをダブルクリックでそのステージへジャンプ
- 「CSV出力」で表全体をCSVファイルに保存(重要アイテムはテキスト内訳で出力)
- ウィンドウのサイズ/位置、および各列の幅は次回起動時に復元されます(列幅はヘッダをドラッグで調整可)

## テストプレイ

「テストプレイ (現在ステージ)」ボタンで、現在のステージから直接エミュレータを起動。事前にエミュレータパス設定が必要。

## 履歴

「履歴」ボタンで過去に開いたROMファイル一覧から選択可能。

## 出力フォーマット

| 形式 | 用途 |
|---|---|
| `.nes` | パッチ済みROM、エミュレータで直接実行可能 |
| `.ips` | 元ROMとの差分パッチ、配布用 |
| `.png` | ステージプレビュー画像 |
| `.xml` | skchain互換ステージファイル（単一/全ステージ）。skchainとの相互運用可 |

PNG出力先: `./exports/level_NN.png`

## ゲーム挙動改造

「編集ツール」→「ゲーム挙動改造...」で、ROMの既知アドレスを書き換えてゲーム挙動を変更できます。

### 編集方針

本ツールのROM改造は **データ値の書換え** を基本方針とし、必要な場合だけ
位置+署名で検証した小さなフックを使います。

- **データ書換え（数値変更）**: 積極的に対応する
- **プログラム命令の書換え**: 必要最小限。既存処理の副作用を残さない形に限定

例: 開始ステージ変更、初期魔法、Room Flag系など。

### 開始ステージ
1〜53面の任意のステージから開始できます。

### 初期魔法
ステージ開始時の魔法状態を共通設定します。旧エディタのような
「デモ/ゲーム開始/コンティニュー」3行拡張は使わず、原作に近い
共通設定として扱います。

| 設定 | 内容 |
|---|---|
| 最大数 | `$042B`。原作は3。0〜8を指定可能 |
| 初期所持 | `$042E/$042F`。`F`=通常ファイヤー、`S`=スーパー。例: `FFF`, `SSS`, `FSFS` |

原作値は「最大数3・初期所持なし」です。原作値に戻すと、追加フックも
元の命令へ復元されます。内部では、火球の経過カウンタ `$042C/$042D`
のゼロクリアは残し、`$042B/$042E/$042F` だけを共通値で上書きします。
v0.6.79 以降は、NEW GAME 初期化の `$9144` とステージ開始初期化の
`$C9E3` の両方を同じ routine へ接続して、実プレイ開始時にも
初期所持が反映されるようにしています。
v0.6.80 以降は、この routine が A レジスタを保存・復元します。
NEW GAME 初期化直後の原作ゼロクリア処理を壊さないための対策です。

### コンティニュー上限ステージ
原作は「ステージ42以降でゲームオーバーすると必ずステージ41に戻される」仕様。
これを変更してコンティニュー可能な範囲を拡大できます。

| 設定 | 効果 |
|---|---|
| ステージ41まで | 原作デフォルト |
| ステージ42〜HIDDEN まで | 段階的に上限拡張 |
| **時間の間まで** | **全ステージ（最終53）コンティニュー可能** |

### クリア画面のキャラ
ステージクリア画面（THANK YOU DANA）で左右に出る2体を差し替えます。

| プリセット | 内容 |
|---|---|
| Fairy (原作) | 妖精・羽ばたき（デフォルト） |
| Golem / Gargoyle速/遅 / Demonhead / Saramandor | 各キャラの固有ループアニメ |

全プリセットは速度ゼロのアニメ状態を選んでいるため、クリア画面で落下・移動せず置物として表示されます（ROM解析で確定）。

### デモプレイのステージ
タイトル画面を放置すると流れる自動デモ（attract mode）のステージを変更します。原作は3面固定。

| 設定 | 効果 |
|---|---|
| OFF | 3面（原作デフォルト）に復元 |
| 3〜53面 | 指定した面でデモを再生 |

- **3面以上のみ**指定可能（内部でレジスタを使い回す構造のため、1〜2面は不可）。
- デモの操作は3面用に録画された固定入力の再生です。別の面にするとDanaが録画通りに動くため、背景が変わって「別の面になった」ことが見た目で分かります（デモ確認用途には十分。通常プレイには一切影響しません）。
- JP/US両対応。位置とバイトパターンの二重検証で、改造ROM・異版では安全に中止します。

### 敵ドロップ編集（グローバル）
左ペインの「敵ドロップ編集...」ボタンで、敵を**炎（ダーナの火球）で
倒した時に出る効果**と確率を全体的に変更できます。

- 10行 × 8枠の表を編集します。確率＝その効果が8枠中いくつ入っているか
  （例: 4枠なら 4/8＝50%）。
- 設定できるのは実機で確定した14種の**効果値**のみ：なし／特殊
  （クリスタル系）／$042B++／ファイア距離+／妖精予約／1UP／
  スコア +10〜+2000。
- **重要**: これは「拾得アイテムAI のドロップ効果値」であり、
  通常アイテムの番号とは別物です。たとえば $06 は **1UP** であって
  **鍵ではありません**。敵に通常アイテム（鍵・壺など）を落とさせる
  には別の仕組み（コード変換層）が必要で、本機能の対象外です。
- 1つの行は複数の敵グループで共有されます（例: ある行を変えると
  Neul 全種に反映）。各行に「使用する敵」を表示しています。
- 「原作に戻す」で完全復元。位置＋署名の二重検証付きで、改造ROM・
  異版・破損では安全に中止します。

### デモ操作編集（attract mode）
左ペインの「デモ操作編集...」ボタンで、タイトル画面を放置すると流れる
デモの操作を編集できます。

- デモは「34ステップ固定の入力データ」を再生する方式です。各ステップ
  ＝入力(A/B/上下左右のチェック)と、それを続けるフレーム数。
  60フレーム≒1秒です。
- 原作の操作が初期表示されます。編集して保存すると差し替わり、
  「原作に戻す」で完全復元します。
- 録画機能ではなく、原作と同じ記述方式を手で入力する形です。
  34ステップ固定なので、大きめの区切り(右へ何フレーム→ジャンプ
  何フレーム…)で組むのが前提です。
- Start/Selectはデモが中断する判定に使われるため選択できません。
- 最後に死ぬ動きは不要です。34ステップを使い切ればデモは終わります
  (最後は放置でも、見せたい動きを作るだけでOK)。
- デモ領域は版で配置が違うため**JP版専用**です(US等は安全に中止)。
  位置＋署名の二重検証付き。

### クリア画面メッセージ編集
左ペインの「クリア画面メッセージ編集...」ボタンで、ステージクリア後の
『おめでとう画面』3行を編集できます。

- 原作の3行: `THANK YOU DANA`(14字)/`YOU RELEASED THIS ROOM`(22字)
  /`TRY NEXT ROOM`(13字)
- **英大文字 A-Z とスペースのみ**。**原作と同じ文字数まで**(長くは
  できません。短い分はスペースで埋まります)= 安全な同字数置換方式
- 原作文字が初期表示。「原作に戻す」で完全復元。OK/キャンセル/適用
- 版で配置が違うため**JP版専用**(US等は署名不一致で安全に中止)。
  各行のヘッダ・終端は変更しない位置＋署名の二重検証付き
- 長さや表示位置を変える編集は安全性のため非対応(将来別途)

### タイトル移植・編集
左ペインの「タイトル移植 (US↔JP)...」ボタンで、別 ROM の
タイトル画面を現在の JP ROM へ移植できます。現在の基本方針は
**JP ROM を読み込み、mapper66 + 内部wide形式へ自動変換して編集する**
方式です。

操作: ベース ROM 読込 → ボタン → 移植元 ROM(.nes/.zip)を選択
→ JP/US 自動判定 → タイトルピースをコピー → 保存可。

仕組み: タイトルは「配置(nametable)」「色区分(attribute)」
「絵(CHR bank3)」という独立データの集合です。現在の JP ROM は
読込時に内部wide形式へ正規化されるため、配置データは PRG bank1 の
wide title workspace に置かれます。これにより、通常のJPタイトルも
US由来タイトルも同じ編集経路で扱います。

- **移植内容**: nametable(配置 402B) + attribute(色区分 21B) +
  CHR bank3(絵 8192B)。JP/US でこれらの長さは完全に同一なので
  安全にそのままコピーできます
- **これは IPS 適用ではありません**。既知ピースのコピーゆえ
  **CRC 一致は不要**(CRC が違っても問題ありません)
- **移植先は JP/JP66**。移植元は JP/US を自動判定
- **プレビュー**: ダイアログを開くと、今読み込んでいる ROM の
  **実際のタイトル画面**がそのまま表示されます(nametable を
  デコードし CHR bank3 で合成、256x240、倍率 x1〜x8)。移植を
  実行すると結果が即座にプレビューへ反映されます
- **画像として保存**: タイトル画面 256x240 を PNG / BMP で出力
- **PNGから取り込む**: 画像(PNG/BMP)からタイトルの絵を取り込み
  ます(「画像として保存」の逆)。256x240/4階調へ自動量子化し
  CHR bank3 を再構築。**配置(nametable)は変えない方式**なので
  圧縮枠を超過せず安全(往復は画素まで厳密一致)。export した
  画像を画像ソフトで描き替えて戻す用途に最適。※同じタイル番号
  を共有する複数マスで絵が食い違う場合は最多のものを採用
  (export画像をそのまま編集すれば食い違いは発生しません)。
  ※配置そのものを画像で変える取り込みは今後の拡張
- ※プレビュー/保存画像は実画面と整列するよう位置補正済み
  (右8px→左・下1px→上の巡回。取り込み時は自動で逆補正するので
  export画像をそのまま編集して戻せます)
- ※プレビューの色は未確定のため白黒4階調表示(形は正確)
- **追加文字**: 「追加文字...」でタイトル中央付近に1行の文字を
  追加できます。A-Z / 0-9 / スペース / `,` / `.` / `"`、最大32文字。
  既存の文字は次回ダイアログを開いた時に読み戻されます。既存の
  `PUSH START` / `TECMO` 文字描画ルーチンは変更せず、wide title
  stream 側に文字タイルを追加します
- 「変更を取り消す」で開いた時点へ復元。OK/キャンセル/適用
- **著作権配慮**: データはツールに一切含めません。ご自分が
  所有する ROM 同士でのみ移植します
- 位置+署名の二重検証付き(両 ROM)。非対応/破損/長さ不一致は
  安全に中止(フォールバックなし)。読込時の拡張 ROM(mapper66)
  でも各ピース位置を正しく扱います
- ※**色(パレット)は現状 移植先のまま**です(配置・絵は移植
  済)。タイトルのパレットは小片が散在しており全特定が未完の
  ため、色の完全再現は今後の改良予定
- ※移植後は**実機/エミュで要確認**してください。タイトル編集は
  見た目だけでなく PRG bank1 の配置データも更新します。

### 原作バグ回避：落下中の横穴侵入を安定化
ゲーム挙動改造ダイアログの「原作バグ回避」チェックで有効化します。

ソロモンの鍵には「上から落ちながら左／右を入れて横穴に入りたいのに、
同じ操作でも入れる時と入れない時がある」という長年の挙動があります。
これは内部のサブピクセル位置（落下の位相）でヒットボックスと横穴の
判定タイミングがズレるためで、プレイヤーには制御できない実質運任せ
でした（このツールの Mesen 実機解析で原因を解明）。

- ONにすると、**横穴が隣接している時だけ**判定を補正し、毎回安定して
  入れるようになります（左右対応）。
- **通常の壁・歩行・ジャンプ・着地は原作どおり**で、移動速度も変えて
  いません（横穴がない場所では一切作用しない＝副作用なし。実機確認済）。
- グローバル設定です（全ステージに適用）。OFFで原作挙動に戻ります。
- 位置＋署名の二重検証付き。Room Flag 系の改造とも同時に使えます。

### 呪文(Page)の出現位置 ※JP のみ
ゲーム挙動改造ダイアログの「呪文(Page)の出現位置」で、特殊面に出る
呪文(Page = item 0x21)のマス座標を変更できます。

- **52面 空間の呪文の位置** / **53面 時の呪文の位置** を単純な X/Y で指定
- 原作値: 52面=(7,2) / 53面=(7,9)。左上が (0,0)
- 内部的には ROM の特殊処理スクリプト（出現マスを指定する1バイト）を
  直接書き換えます。位置＋署名の二重検証付き（不一致時は安全に中止）
- キャンバス上の Page マーカーをドラッグしても同じ位置を変更できます
  （どちらも同じ ROM 箇所を編集。併用可）
- US/US66 版は該当箇所が未対応のため、この欄は自動で無効になります
F9で設定ダイアログを開きます。OK／キャンセル／適用の3ボタン構成。

| 項目 | 内容 |
|---|---|
| 未保存マーク | タイトルバーの未保存表示記号 |
| フォント | フォントファミリー（「既定に戻す」でアプリ標準） |
| フォントサイズ | 0=デフォルト、1pt〜任意 |
| 太字 | アプリ全体のフォントを太字化 |
| アイコン | ウィンドウアイコン画像のパス |
| エミュレータ | 連携するエミュレータのexeパス |

フォント設定はアプリ全体に即時反映されます（適用ボタンで閉じずに確認可能）。通知音・クラウドバックアップ先は今後追加予定。

## 制限事項

- **デーモンミラーのドロップスケジュール編集UI**は未実装（敵セットはメインウィンドウのミラーパネルで編集可能）
- **特殊処理 (Per-Room Special Process) の Canvas 編集 UI** は未実装（ビューワのみ。今後 v0.3.x で対応予定。詳細は `docs/future_plan.html`）
- 敵は1ステージあたり15体まで（拡張ROMでも変わらず、ROMフォーマット由来の制約）

## トラブルシューティング

### ROM読込エラー「Unknown ROM region」
ROMが Solomon's Key (NES) でないか、対応リージョン (US/JP/EU) でない可能性。

### 起動できない (Windows)
Visual C++ ランタイムをインストール:  
https://aka.ms/vs/17/release/vc_redist.x64.exe

### 保存時エラー「Item data overflow」
ステージにアイテムを置きすぎ。容量に収まるよう削減してください (拡張ROMでは緩和)。

## 移植元

[skchain v1.1](https://github.com/kaimitai/skchain) by kaimitai (Kai E. Frøland)

ROM解析と設定ファイルは原作者の素晴らしい仕事に依存しています。
## v0.6.84 追加メモ

- **52/53面のPage位置**: ゲーム挙動改造ダイアログからは外しました。
  キャンバス上の Page マーカーを Ctrl+左ドラッグで移動してください。
  内部的には同じROM位置を書き換えるため、二重UIによる上書き事故を避けるためです。

## v0.6.83 追加メモ

- **ゲーム挙動改造**: 項目数が増えたため、設定画面を `基本` / `プレイヤー` /
  `敵・AI` / `画面・演出` / `保守・特殊` のタブに整理しました。
  ROMへ書き込む内容は v0.6.82 から変えていません。

## v0.6.82 追加メモ

- **初期残数**: ゲーム挙動改造ダイアログから、開始時のダーナ残数を変更できます。
  原作は3。残数RAM `$0452` だけを変更し、巻物最大数 `$042B` には影響しません。
